<fieldset>
  {{ formGroup condition.field name="condition" value=condition.value choices=condition.choices localize=false
  label=condition.label blank=condition.placeholder }}
  {{ formGroup apply.field name="apply" value=apply.value localize=true label=apply.label }}
</fieldset>